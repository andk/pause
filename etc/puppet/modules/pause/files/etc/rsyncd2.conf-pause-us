max connections = 8
log file = /var/log/rsyncd2
pid file = /var/run/PAUSE-rsyncd2.pid
transfer logging = true
use chroot = true
port = 8732
timeout = 1200

[PAUSE]
    path = /home/ftp/pub/PAUSE
    auth users = *
    secrets file = /etc/rsyncd.passwd

[opt]
    path = /opt
    auth users = andk
    secrets file = /etc/rsyncd.passwd
